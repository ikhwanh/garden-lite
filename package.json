{
  "name": "garden-lite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first garden manager with GitHub Gist sync (Lit + Dexie)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lit-labs/router": "^0.1.4",
    "dexie": "^4.0.10",
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "sass": "^1.83.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vite-plugin-pwa": "^1.3.0"
  }
}
